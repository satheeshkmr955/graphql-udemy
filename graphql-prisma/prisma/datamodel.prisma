type User {
  id: ID! @id @unique
  name: String!
  email: String! @unique
  posts: [Post]!
}

type Post {
  id: ID! @id @unique
  title: String!
  published: Boolean!
  body: String!
  author: User!
}